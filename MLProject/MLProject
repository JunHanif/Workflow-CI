name: Hantavirus_Retraining

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir: {type: str, default: "hantavirus_dataset_preprocessing"}
    command: "python modelling.py {data_dir}"